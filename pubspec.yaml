name: smart_disaster_management
description: Smart Disaster Management Admin Dashboard for Flutter Web

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6


  cd D:\Projects firebase_auth: ^5.1.4
  firebase_storage: ^12.1.3


  # Recommended additions for full feature set:
  # fl_chart: ^0.68.0          # Charts & analytics graphs
  # flutter_map: ^6.1.0        # Interactive map widget (OSM-based, free)
  # latlong2: ^0.9.0           # Lat/lng types for flutter_map
  # http: ^1.2.0               # API calls to your backend
  # provider: ^6.1.0           # State management
  # intl: ^0.19.0              # Date formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
